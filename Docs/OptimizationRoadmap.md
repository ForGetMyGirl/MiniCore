# MiniCore 优化路线图

本文档记录 MiniCore 后续优化的执行顺序、设计边界和验收标准，供后续按阶段实施。优先以压测数据和线上指标定位瓶颈，不以“代码看起来可能更快”作为改动依据。

## 使用方式

1. 先完成阶段 0 的基线测试和监控。
2. 每次只实施一个阶段或一个可独立回滚的子任务。
3. 实施前后对比相同场景下的吞吐、P95/P99 延迟、主线程耗时和 GC。
4. 达到验收标准后再进入下一阶段；若收益不明显，记录结论并暂停该方向。

## 已确认优化记录

### 2026-08-01：完整 39 条回归全部通过，冻结为正式性能基线

| 项目 | 记录 |
| --- | --- |
| 前置与报告 | Editor 三协议回环通过；Android Development 完整报告为 `~/AndroidBenchmark/20260801_124241/`，UTC `2026-08-01 12:42:41` / 北京时间 `20:42:41`，共 `39` 条。 |
| 可靠性 | 全部样本零失败、零未收、零出站/入站拒绝、零断线；主线程停顿恢复 TCP `12.605 ms`、KCP `37.217 ms`、UDP `12.853 ms`，均通过。 |
| 中负载质量门槛 | 九条 `1000/s` 的 P99：TCP `39.038–39.315 ms`、KCP `48.600–48.784 ms`、UDP `38.661–38.907 ms`，均低于 `50 ms`。入站等待 P99 分别为 `37.250`、`40.000`、`36.000–36.250 ms`；单包处理 P99 均为 `0.250 ms`。 |
| 传输回归 | TCP `5000/s` 约 `300,000` 帧对应约 `24,100–24,300` 次接收操作，连续拆包保持；UDP `5000/s` 约 `300,000` 逻辑包对应约 `50,500` 次实际写入，约 `5.9` 包/datagram，合包保持。 |
| 结论 | 本条取代此前 `20260801_110649`，正式冻结为当前性能基线；旧完整场景的共同 P99 超标在当前构建未复现。由于两次运行条件差异没有被单独控制，不能把这次通过归因于某一项新修复。保留分段指标，未来只在完整回归重新失败时继续定位。 |

### 2026-08-01：中负载分段诊断三轮稳定通过，先复现完整场景差异

| 项目 | 记录 |
| --- | --- |
| 三轮报告 | `~/AndroidBenchmark/20260801_114351/`、`20260801_115133/`、`20260801_115843/`；UTC `11:43:51`、`11:51:33`、`11:58:43`。 |
| 可靠性 | 九条样本均零拒绝、零失败、零未收、零断线；每种传输每轮均约 `60000` 成功投递并到达真实 Handler。 |
| 稳定性 | 端到端 P99 的三轮范围：TCP `38.048–38.812 ms`（跨度 `0.765 ms`）、KCP `47.808–48.139 ms`（`0.331 ms`）、UDP `37.161–37.802 ms`（`0.642 ms`），均低于 `50 ms`。最小中负载场景已稳定通过。 |
| 分段定位 | 三种传输的入站等待 P99 分别为 TCP `36.250–37.000 ms`、KCP `38.500–39.750 ms`、UDP `34.500–35.750 ms`；单包处理 P99 仅 `0.250–0.500 ms`。端到端尾延迟主要在网络入队到主线程开始处理的交接，不是单包处理。 |
| 下一步 | 不修改网络实现。使用当前带分段指标的构建复跑完整 `39` 条，区分“完整场景前序负载或运行条件导致的差异”与可复现的入站主线程交接抖动；只有失败被当前报告复现后，才添加帧级关联诊断。 |

### 2026-08-01：中负载分段诊断首轮定位入站主线程交接，不判为稳定修复

| 项目 | 记录 |
| --- | --- |
| 真机报告 | `~/AndroidBenchmark/20260801_114351/`，UTC `2026-08-01 11:43:51` / 北京时间 `19:43:51`；最小诊断共 `3` 条。 |
| 可靠性与门槛 | TCP/KCP/UDP 均 `60000` 尝试、接受和接收，零拒绝、零失败、零未收、零断线；端到端 P99 分别为 `38.048`、`47.808`、`37.161 ms`，本轮通过。 |
| 分段证据 | 入站等待 P99 分别为 TCP `36.250`、KCP `38.500`、UDP `34.500 ms`；主线程单包处理 P99 仅 `0.500`、`0.250`、`0.250 ms`。本轮端到端尾延迟主要落在“网络线程入队 → 主线程开始处理”，不是单包反序列化或 Handler。 |
| 边界解释 | Handler 仍按设计在主线程执行；该指标表明帧调度交接是端到端计时的主要组成，不等于网络 I/O 或收发线程被主线程阻塞。 |
| 状态 | 这是一轮通过样本的定位证据，**不**覆盖完整 `39` 条九条失败，也**不**构成稳定性或修复结论。保持实现不变再运行两轮相同最小诊断；只有失败被复现后，才按同一分段指标决定是否增加帧级/序列号关联诊断。 |

### 2026-08-01：完整 39 条回归确认共同中负载 P99 待定位

| 项目 | 记录 |
| --- | --- |
| 功能前置 | Editor `NetworkLoopback_AllTransports_UseActualHandlersAndProtobuf` 通过，覆盖 TCP/KCP/UDP 的连接、`SendAsync`、16 条 `TrySend`、RPC 与断线。 |
| 真机报告 | `~/AndroidBenchmark/20260801_110649/`，UTC `2026-08-01 11:06:49` / 北京时间 `19:06:49`，Android Development Player 的完整 `39` 条本机回环。 |
| 可靠性 | 所有 `39` 条均零失败、零未收、零出站/入站拒绝、零断线；三种主线程停顿恢复均低于 `50 ms`。 |
| 失败样本 | 九条正常消息 `1000/s` 的 P99 全部超标：TCP `69.970–70.054 ms`、KCP `85.193–85.430 ms`、UDP `80.480–80.983 ms`。这是完整回归失败的唯一质量门禁原因。 |
| 已排除 | TCP `1000/s` 服务端 `60,011` 完整帧只发生 `9,639` 次 Socket 接收，连续拆包没有回退；UDP `5000/s` 约 `300,000` 逻辑包仅约 `50,400` 次实际写入，`TrySend` 合包没有回退。不能据此直接调整批次、队列容量、线程模型或 P99 门槛。 |
| 统计结论 | P50/P95/P99 同源于成功 `TrySend` 到真实 Handler 业务事件的一组端到端样本，并由同一预分配数组排序计算；不存在 P99 单独走新发送路径、P50 仍走旧路径的实现。 |
| 后续诊断 | 增加入站等待与主线程单包处理的固定时间桶 P50/P95/P99，仅压测开启；使用三协议各一轮 `1000/s` 的最小诊断重现。若两个分段均低于端到端 P99，才继续补充发送后、传输写入后和接收入队前的序列号分段。 |

### 2026-08-01：UDP TrySend 数据报合包消除逐 datagram 串行上限

| 项目 | 记录 |
| --- | --- |
| 原始证据 | `~/AndroidBenchmark/20260801_085337/` 的 UDP `5000/s`：`299894 / 76003 / 223891 / 76003`（尝试/接受/拒绝/接收），P99 `866.241 ms`，入站队列拒绝为零。客户端出站队列等待平均 `804.297 ms`，而单次 UDP 传输写入平均仅 `0.617 ms`。 |
| 已确认根因 | 同一会话的发送泵为保持顺序，逐个业务包等待一次 UDP `Socket.SendToAsync` 完成。每次写入本身不慢，但约数百微秒的串行成本不足以支撑 `5000/s`；不是 Handler、入站队列、收包线程或“还需要新发送线程”。 |
| 实施方案 | 仅对 `TrySend` 普通数据队列，从当下已存在的待发包立即收集，最多 `16` 包且一个 datagram 不超过 `1200 B`，编码为 `MCUB` v1 批量 datagram 后单次写出；不等待凑包，只有一个包或大包时保持 raw 单包。`SendAsync`、RPC、心跳和可靠控制队列保持单业务包单 datagram 与原有等待完成语义。 |
| 接收与语义 | 接收端先验证完整 `MCUB` datagram，成功后再依序交付原业务包。一个批量 datagram 的 UDP 丢失会同时丢失其中全部 `TrySend` 状态消息，因此该路径只给可丢失的高频状态使用。`MCUB` 是协议扩展，客户端/服务端必须同步升级；不可将混合版本视作批量通信兼容。 |
| 真机验证 | `~/AndroidBenchmark/20260801_092609/`：UDP `5000/s` 为 `300000 / 300000 / 0 / 300000`、`4993.174/s`、P99 `46.647 ms`，零失败、零未收、零入站拒绝、零断线。`300012` 个逻辑出站计时样本仅产生 `50705` 次实际传输写入，约 `5.9` 包/datagram，直接验证合包生效。 |
| 后续 | UDP `5000/s` 专项已解决，既有 UDP `1000/s` 不重跑。下一步执行三协议 `NetworkLoopbackIntegrationTests` 覆盖 `SendAsync`、`TrySend`、RPC 与断线混合路径；完整 `39` 条三轮基线仅留给发布前或共享网络核心继续变更后。`MaxGcAllocatedBytesPerFrame` 的单帧峰值需另行用分配诊断定位，不能仅凭一个峰值直接改动池或线程模型。 |

### 2026-08-01：TCP 连续收包拆包消除每包双 Socket 等待

| 项目 | 记录 |
| --- | --- |
| 原始证据 | `~/AndroidBenchmark/20260801_082139/` 的 TCP `5000/s`：`300000 / 142786 / 157214 / 132726`（尝试/接受/拒绝/接收），吞吐 `2140.175/s`、P99 `6832.556 ms`。服务端完整帧/回调均为 `132778`，入站队列拒绝为零；但 Socket 接收操作为 `265557` 次，即约每帧两次，平均 `0.217 ms`。 |
| 已确认根因 | 旧 TCP 读循环每包都串行 `ReceiveAsync(4 字节包头)` 再 `ReceiveAsync(正文)`。两次异步等待的累积速率上限造成服务端读取不足，并通过 TCP 流控反压到客户端 Socket 写入；不是 Handler、固定入站队列、发送线程池或全局队列容量。 |
| 实施方案 | 以固定 `64 KiB` 连续接收缓冲读取 TCP 字节流，一次接收后循环解析全部完整帧；仅有跨读取的大帧时保留未完成字节并按协议最大包长扩容。完整帧仍复制进独立租用数组后按原顺序回调，协议、所有权和断线语义不变。 |
| 真机验证 | `~/AndroidBenchmark/20260801_083926/`：TCP `1000/s` 为 `60000 / 60000 / 0 / 60000`、P99 `38.098 ms`；TCP `5000/s` 为 `300000 / 300000 / 0 / 300000`、`4995.950/s`、P99 `38.159 ms`。两档均零失败、零未收、零入站拒绝、零断线。服务端 `5000/s` 接收操作降至 `24670 / 300012`，证明一次读取可解析多帧。 |
| 保留观测 | 压测启用服务端 `ServerTransportReceiveOperation*`、客户端 `ClientSocketSendOperation*`；只在诊断期开销计时，正式业务默认关闭。 |
| 后续 | TCP 专项问题已解决。下一步不再重复 TCP 这两条快速测试；先按条目优化 UDP `5000/s`，并只在发布前或 TCP 传输再次变更后补三协议冒烟与三轮长期基线。 |

### 2026-07-31：TCP 单帧合并验证有效，进入有界多包批量

报告目录为 `~/AndroidBenchmark/20260730_161646/`；UTC 生成时间为 `2026-07-30 16:16:46`，即北京时间 `2026-07-31 00:16:46`。本轮只运行 TCP `1000/s`、`5000/s` 两条专项回归，用来验证将单包长度头与正文合并为一次写入的效果。

| 条目 | 结果 | 证据与结论 |
| --- | --- | --- |
| TCP 普通 `1000/s` | 吞吐与可靠性通过，延迟尚未达标 | `60000 / 60000 / 0 / 60000`（尝试/接受/拒绝/接收），实际 `999.273/s`；客户端传输平均 `0.864 ms`。但 P99 `61.652 ms` 高于中负载 `50 ms` 验收线，不能冻结为正式基线。 |
| TCP 普通 `5000/s` | 未通过 | `300000 / 65678 / 234322 / 60318`；实际 `972.769/s`，P99 `8195.184 ms`，停止投递后的 `5360` 条未在两秒排空窗口到达 Handler。入站拒绝为零，客户端出站等待平均 `928.021 ms`，单次传输写入平均 `0.912 ms`。 |

与上一轮 `5000/s` 的 `42,371` 接受数相比，单帧合并提高到 `65,678`，证明“两次 TCP 写入”的确是成本之一；但约 `0.9 ms` 的单次写出仍把逐包串行能力限制在约 `1,000/s`。因此已实施下一步：仅 TCP 普通数据队列把已完成的长度帧复制进池化临时数组后合并写出，单批最多 `32` 包、`32 KiB`。可靠发送、RPC、心跳保持逐包优先；UDP/KCP 协议和发送路径不变。批量写入失败时，整批完成源收到同一异常，所有原始缓冲和临时批量缓冲均归还。

同时，压测质量门禁已补齐 `1000/s` P99 `<= 50 ms` 检查。此前运行器只检查拒绝、失败、丢失、断线与停顿恢复，可能把中负载 P99 超标误显示为通过；该行为现在与性能指南的书面验收标准一致。

### 2026-07-30：剩余高频普通消息快速回归，定位到底层逐包写入

报告目录为 `~/AndroidBenchmark/20260730_155830/`；其 UTC 生成时间为 `2026-07-30 15:58:30`，即北京时间 `2026-07-30 23:58:30`。这是线程池出站泵改动后的单轮快速回归，不足以替代三轮正式基线。

| 条目 | 结果 | 证据 | 状态 |
| --- | --- | --- | --- |
| TCP 普通消息 `1000/s` | 未通过 | `60,000` 次尝试中接受 `45,452`、拒绝 `14,548`；实际 `739.426/s`，P99 `1471.002 ms`；客户端出站排队平均 `1344.333 ms`，而单包传输写入平均仅 `1.320 ms`。 | 继续优化。 |
| TCP 普通消息 `5000/s` | 未通过 | 接受 `42,371`、拒绝 `257,629`；实际 `689.262/s`，P99 `1497.411 ms`，客户端出站排队平均 `1442.381 ms`。 | 继续优化。 |
| UDP 普通消息 `1000/s` | 单轮通过，待三轮基线 | `59,999` 次尝试全部接受和接收，无拒绝/失败/丢失；实际 `999.407/s`，P99 `45.823 ms`。 | 不再作为当前优化对象；正式冻结前补三轮。 |
| UDP 普通消息 `5000/s` | 未通过 | 接受 `91,364`、拒绝 `208,636`；实际 `1504.637/s`，P99 `738.276 ms`，客户端出站排队平均 `665.088 ms`，单包传输写入平均 `0.654 ms`。 | 继续优化。 |
| TCP / UDP 主线程停顿 | 单轮通过，待三轮基线 | 均无拒绝、失败、丢失或断线；停顿后队列恢复分别为 `40.915 ms`、`20.044 ms`，均低于 `50 ms` 验收线。停顿期间的约 `2 s` 端到端延迟是场景刻意注入，不判为失败。 | 不随普通消息吞吐继续重复；改变相应传输实现后再复核。 |

代码与分段指标共同限定了下一步：线程池方案已经排除“发送续体回主线程”的问题；当前每个会话仍严格逐包等待底层发送完成。该报告版本的 TCP 长度前缀和正文至少经过两次异步写入，UDP 每个应用包也对应一次独立 datagram 写入。已先实施不改变协议的 TCP 单帧合并实验：小包将长度头与正文复制进池化数组后一次写入，大包保持分段写入。下一轮只复测本表 TCP 条目，以决定是否有必要进入多包有界批量；不把 TCP 字节流方案错误套用于 UDP，也不因无证据新增全局专用发送线程。

### 2026-07-30：客户端出站泵脱离 Unity 主线程

| 项目 | 记录 |
| --- | --- |
| 根因证据 | Android 64 并发 RPC 分段诊断显示，TCP/UDP 的主要耗时是客户端出站队列等待和发送完成后的 Unity 主线程续体调度；不是固定队列容量或缓冲池。 |
| 实施方案 | 保持每个 `NetworkSession` 唯一、串行的出站发送泵；仅将 `DrainAsync` 及其底层发送完成续体切换到共享 `MTaskExecutors.ThreadPool`。业务 Handler 和 Unity 游戏状态仍在主线程。 |
| 顺序与所有权 | 同会话仍一次只处理一个包，跨线程不会改变 TCP/UDP/KCP 的会话内发送顺序；发送缓冲在底层发送完成、异常或断线后才归还。不同会话不应互相排队。 |
| Android 验证 | `20260730_150625-threadpool`：TCP P99 `1431→101 ms`、UDP P99 `501→72 ms`；KCP P99 `102→103 ms`，三种传输均零失败、零未收、零拒绝、零断线。 |
| 不采用方案 | 不将全部发送泵放入既有 `MTaskExecutors.Network`，该方案已实测与收包/KCP 更新争用并严重回退；也不在没有容量证据时创建一条全局专用发送线程。 |
| 后续触发条件 | 仅当多会话压测显示线程池排队、CPU 利用率或 P99 抖动成为瓶颈时，评估按会话哈希分片的 I/O 执行器；不采用所有会话共用一条发送线程的全局串行模型。 |

### 2026-07-29：按条目冻结 Android 结果，避免无效全量复测

| 状态 | 条目 | 结论 |
| --- | --- | --- |
| 已冻结 | TCP 普通消息 `100/s`；KCP 普通消息 `100/1000/5000/s`；KCP 主线程停顿；UDP 普通消息 `100/s`。 | 已完成三轮或指定场景验证；后续不因 RPC 压测执行器改动而重测。 |
| 待优化 | TCP 普通消息 `1000/5000/s`；UDP 普通消息 `5000/s`。 | `20260730_155830` 已排除主线程续体问题；剩余拒绝来自会话内逐包等待底层写出。 |
| 待重测 | TCP/KCP/UDP RPC；TCP/UDP 主线程停顿。 | 本轮 RPC 使用的 `MTask.Yield()` 不能形成真实调度间隔，UDP 达到保护上限，相关条目不作为结论。修复后按 RPC 快速回归、RPC 专项基线顺序运行。 |

本表是 `2026-07-29` 当时的冻结快照：其中 TCP `1000/5000/s` 已由上方“TCP 连续收包拆包”记录通过，UDP `5000/s` 已由上方“UDP TrySend 数据报合包”记录通过；不得再把本表的“待优化”当作当前状态。RPC 与主线程停顿的待重测结论仍然有效。

### 2026-07-28：固定容量入站/出站队列与 Android 压测校准

| 项目 | 记录 |
| --- | --- |
| 入站 | 用预分配、`lock` 保护的固定环形队列替换正式收包 `ConcurrentQueue`；普通全局预算 `4096` 包/`4 MiB`，单会话 `1024` 包/`1 MiB`，控制包有保留容量。持续满三秒断开异常会话。 |
| 出站 | 每会话单一发送器串行写入传输；`SendAsync`/RPC/心跳走可靠保留队列，`TrySend` 供高频普通消息立即入队。数据预算 `1024` 包/`1 MiB`，持续满三秒断开。 |
| 缓冲 | 旧 `ConcurrentStack` 缓冲池 GC 已消除；Protobuf 正式路径直接写入租用的完整包数组，所有结束路径归还。 |
| Android 首跑 | 39 条本机回环样本功能通过；TCP/UDP 普通消息受逐条等待 `SendAsync` 限速，不能作为吞吐基线。 |
| 后续验收 | 重跑 Android，报告 offered/accepted/rejected/received、入站/出站峰值、P99、GC、拥堵断线和恢复时间；新增真正并发的固定队列 GC 压测。 |

### 2026-07-15：关闭日志时的网络热路径跳过字符串构造

| 项目 | 记录 |
| --- | --- |
| 优化范围 | `LogSwitch` 默认开关策略，以及 `NetworkService` 的收包、普通发送、RPC 请求和 RPC 响应日志路径；仅调整日志关闭时的热路径，不改变网络包处理语义。 |
| 原始问题 | 旧路径在 `LogSwitch.EnableLog = false` 时仍会先格式化时间、构造插值日志字符串，再进入已关闭的 `LogSwitch.Info`。日志输出被抑制，但每包字符串与时间格式化成本已经发生。 |
| 基线与归因证据 | `NetworkIncomingLogPerformanceTests.cs` 使用相同日志文本对比旧写法与候选写法，均在日志关闭状态下每组执行 `10,000` 次。旧路径 `IncomingLog_BuildsStrings_WhenLoggingDisabled` 三次中位值为 `22.116 / 22.168 / 22.318 ms`，每次 `GC() = 10.0005`；优化路径 `IncomingLog_SkipsStrings_WhenLoggingDisabled` 三次中位值为 `0.034 / 0.036 / 0.036 ms`，每次 `GC() = 0`。 |
| 实施方案 | `LogSwitch` 在 Editor/Development 默认开启、正式非开发构建默认关闭；`NetworkService` 的收包、普通发送、RPC 请求和 RPC 响应日志均先判断 `EnableLog`，再格式化时间和字符串；Payload 日志同时要求 `EnableLog && EnablePayloadLog`。 |
| 前后性能数据 | 关闭日志时的收包日志热路径从旧路径中位数约 `22.168 ms / 10,000 次` 降至优化路径约 `0.036 ms / 10,000 次`，约减少 `99.84%`；`GC.Alloc` 采样从稳定 `10.0005` 归零。 |
| 测试状态 | 框架阶段性能验证已确认；尚未做真实业务网络冒烟验证。 |
| 残余风险 | 日志开启时仍会保留诊断字符串构造成本；正式包默认关闭日志可避免热路径分配，但需要确认外部调试流程不会依赖正式非开发构建默认输出网络日志。 |
| 下一步 | 在真实 TCP/UDP/KCP 收发场景做一次业务网络冒烟验证，并继续观察 Payload 日志只在显式开启时转换正文。 |

### 2026-07-15：收包缓冲池稳定 GC 消除

| 项目 | 记录 |
| --- | --- |
| 优化范围 | `Network/Transport/Entity/Common/ByteBufferPool.cs`，不改变 TCP、UDP、KCP 和网络组件对 `Rent/Return` 的调用方式。 |
| 原始问题 | 旧实现使用 `ConcurrentStack<byte[]>` 保存空闲数组。数组本身被复用，但每次 `Push` 会产生短命的并发容器内部节点。 |
| 归因证据 | 10,000 次、512 B 的 `Buffer.BlockCopy` 为 `0.156 ms / GC() = 5`；`ConcurrentQueue` 入出队为 `0.464 ms / GC() = 5`；旧缓冲池仅 `Rent/Return` 为 `1.506 ms / GC() = 10005`。因此稳定分配来自缓冲池容器，而非复制或收包队列。 |
| 实施方案 | 每个大小桶替换为内部加锁的 `byte[][]` 槽位栈；稳定阶段只读写已有数组引用，不创建并发栈节点。外部调用方不需要加锁。 |
| 容量边界 | 最大池化单数组 1 MB，单桶最多保留 8 MB，所有桶合计最多保留 32 MB；桶满或超大数组归还时不缓存，避免峰值内存长期滞留。 |
| 性能结果 | 新缓冲池仅 `Rent/Return` 三次为 `1.391 / 1.398 / 1.399 ms`，中位数约 `1.398 ms`，较旧基线约快 `7%`；`GC()` 从 `10005` 降至 `5`。 |
| 端到端结果 | 完整收包交接旧基线中位数约 `3.201 ms / GC() = 10005`；新实现三次为 `2.983 / 3.007 / 3.039 ms`，中位数约 `3.007 ms / GC() = 5`，耗时约降低 `6%`。 |
| 验证状态 | 性能测试已确认，静态 C# 编译与差异格式检查通过；`ByteBufferPoolTests` 已新增，仍应在 Unity EditMode Test Runner 中手动运行一次。 |
| 残余风险 | 首次预热、桶扩容或工作负载超过容量上限时仍会创建 `byte[]`，这是池容量不足的可观测信号；后续应增加运行时命中率、桶满丢弃和保留字节数统计。 |

## 当前基础

以下能力已经具备，不应在没有性能证据时重复改造：

| 能力 | 当前实现 | 后续关注点 |
| --- | --- | --- |
| 收包跨线程转发 | `NetworkService` 使用预分配、`lock` 保护的固定环形队列；网络线程只入队，Unity 主线程在 `Update` 中处理。 | 全局、单会话和控制包预算已落实；后续以真实业务压测调优阈值。 |
| 出站发送调度 | 每会话唯一发送泵在共享线程池等待底层异步写入；同会话有序、不同会话可并行，Unity 主线程只负责业务调用与入队。 | 监控线程池排队、每会话出站等待和 Socket 发送耗时；不能退回主线程或合并为全局单发送线程。 |
| 收包缓冲复用 | `EnqueueIncoming` 从 `ByteBufferPool.Shared` 租用数组，处理完成后归还；每桶使用内部加锁的数组槽位栈。 | 单数组 1 MB、单桶 8 MB、全局 32 MB 上限已落实；后续补运行时命中率和容量统计。 |
| Handler 派发 | `AMHandler`、`ARpcHandler` 通过非泛型 invoker 契约直调，避免每包 `MethodInfo.Invoke` 和 `object[]` 参数数组。 | 启动阶段仍会反射扫描；是否生成直连注册代码由压测决定。 |
| Opcode 兼容性 | 使用稳定 manifest 和生成的 `OpcodeRegistry.Generated.cs`。 | 构建校验和协议版本治理应持续维护。 |
| 组件生命周期 | `Global` 通过 owner 记录直接引用计数，提供 `Get/GetOrAdd/Remove/ReleaseAll/Pin/Unpin/ForceRemove`。 | 增加泄漏诊断和常驻组件审计。 |

## 阶段 0：建立性能基线与观测

### 目标

让所有后续优化都有可比较的数据，先确定瓶颈在网络、序列化、业务 Handler 还是 Unity 主线程。

### 实施项

- 建立三类压测场景：Handler 微基准、Transport 基准、端到端业务场景。
- 为网络层增加可开关的统计器，避免正式环境每包产生字符串或日志。
- 每秒采集以下指标：
  - 收包/发包数量与字节数。
  - 入站队列当前长度、峰值长度和排队时长。
  - 每帧网络处理消息数、处理耗时和 `GC Alloc/Frame`。
  - RPC 成功、超时、取消和断线失败数量。
  - TCP、UDP、KCP 的 RTT、重传率、丢弃率和断开次数。
- 使用 Unity Profiler、Memory Profiler 和独立压测程序分别记录主线程、网络线程与 GC 数据。

### 验收标准

- 能稳定复现低压、中压、高压三档负载。
- 每次报告都包含平均值、P95、P99 和峰值，不只看平均 FPS。
- 可以定位一次“主线程卡顿导致网络积压”的完整链路。

## 阶段 1：入站队列背压与主线程预算

### 问题

当前 `NetworkService.EnqueueIncoming` 会持续向 `incomingPackets` 入队，而 `ProcessQueueAsync` 会在主线程顺序处理直到队列清空。主线程卡顿、恶意流量或 Handler 变慢时，队列可能无限增长，延迟和内存占用会相互放大。

### 推荐方案

1. 为全局入站队列增加最大消息数和最大字节数。
2. 为每个 Session 增加独立的消息数、字节数或速率上限，防止单连接挤占全部预算。
3. 每帧设置处理预算，例如最大消息数或最大毫秒数；预算耗尽后留到下一帧。
4. 给协议定义优先级：断开通知、心跳、RPC 响应优先于可丢弃的普通同步消息。
5. 明确超限策略：丢弃可丢消息、暂停读取、限速或断开异常 Session；不能默认无限缓存。
6. 队列项被拒绝或丢弃时，必须立即归还 `ByteBufferPool` 数组。

### 不要做

- 不要在网络接收线程等待主线程处理完成，会把网络读取、KCP 更新和断线检测一起阻塞。
- 不要将所有消息一律丢弃；RPC 响应和断连消息需要不同策略。
- 不要仅用“每帧最多处理 N 条”而不记录积压字节数。

### 验收标准

- 主线程暂停数秒后，内存和队列长度有明确上限。
- 单个恶意 Session 不影响其他 Session 的 P95 延迟。
- 正常负载下不引入明显吞吐下降，且缓冲池没有泄漏。

## 阶段 2：RPC 生命周期与超时调度

### 问题

大量并发 RPC 的超时、断线失败、取消和回调可能造成定时任务、异常对象和锁竞争压力。

### 实施项

- 为每个 Session 和全局 RPC 表设置最大 pending 数量。
- 将 RPC 超时纳入统一调度器或时间轮，避免每个请求独立创建计时对象。
- 为请求记录截止时间、取消原因和可选的链路追踪 ID。
- 断连时批量失败 pending RPC，并统计失败原因。
- 对重复 rpcId、未知响应、过期响应输出采样日志和计数器。

### 验收标准

- 高并发超时场景下没有明显的定时器对象暴涨。
- Session 断开后 pending RPC 在确定时间内完成失败，不残留条目。
- RPC P99 延迟、超时率和队列积压可以关联分析。

## 阶段 3：缓冲池与数据所有权治理

### 问题

2026-07-15 的微基准确认旧版 `ConcurrentStack<byte[]>` 会在每次归还数组时产生内部节点分配：`10,000` 次 `Rent/Return` 的 `GC()` 为 `10005`。当前已替换为每桶内部加锁的 `byte[][]` 槽位栈，并设置单数组 1 MB、单桶 8 MB、全局 32 MB 的保留上限；峰值时的首次扩容和容量超限仍需要通过指标观察。

### 实施项

- 保持每桶缓存数组数和池总字节预算的边界测试，防止后续修改突破当前上限。
- 记录租用、归还、命中率、未归还数量、桶满丢弃和大包次数。
- 统一所有权规则：租用者负责在 `finally` 中归还；转交队列后由消费者负责归还。
- 审查 TCP 拼包、UDP 收包、KCP 重组和发送路径，避免额外 `byte[]`、`ToArray` 和字符串中间值。

### 验收标准

- 高峰后内存可回落到 32 MB 总保留预算附近。
- 异常、断线、反序列化失败和丢包路径均能归还缓冲区。
- Pool 命中率、分配次数和大包行为可观测。

## 阶段 4：Protobuf 协议演进与 JSON 对比基线

### 问题

当前正式网络路径使用 Protobuf。JSON 仍适合迁移验证、调试或低频兼容场景，但会带来字符串、对象图和 CPU 开销；不能再被视为网络默认实现。

### 推荐策略

1. 以 `ProtobufSerializer`、Proto 角色生成和 Parser Registry 作为正式协议链路。
2. 保留 `NewtonsoftJsonSerializer` 与 JSON 性能测试作为迁移对比，不将其重新设为默认 serializer。
3. 保持 12 字节外层包格式、Opcode 语义和 `RpcId` 头字段稳定；业务字段只在 Proto Body 中演进。
4. 为同一消息持续记录 JSON 与 Protobuf 的包大小、序列化耗时、反序列化耗时、完整入站包和 RPC 的 GC 对比。

### 验收标准

- Protobuf 正式路径在目标设备上有可重复的 CPU、GC 与带宽基线。
- JSON 对比实现不会影响 Player 默认网络路径，也不会破坏现有 Proto/Opcode 兼容性。
- 业务层不需要感知 Transport 差异。

## 阶段 5：UDP/KCP 服务端韧性

### 实施项

- 限制单 IP 的建连频率、Session 数量和包速率。
- 限制单个 datagram、重组包和 KCP 缓冲区的最大尺寸。
- 对未知 conv、畸形包和过期 endpoint 快速拒绝，避免高成本解析。
- Session 超时清理改用分桶扫描或时间轮，避免连接数增大后全表扫描。
- 记录 RTT、重传、乱序、丢弃、握手失败和异常断开指标。

### 验收标准

- 异常 UDP 流量不会导致 Session 表或内存无上限增长。
- KCP 在预期丢包率下的 P99 延迟、重传率和 CPU 使用有基线数据。

## 阶段 5.5：按证据演进客户端与专用服务器 I/O 执行模型

### 当前客户端边界

- Unity 主线程负责游戏状态、最终 Handler、轻量 Protobuf 封包和向有界队列投递；不得等待 Socket I/O。
- 网络专用执行器负责收包、拆包及可在线程安全阶段完成的协议处理；它只把业务包交给入站队列，不调用 Unity API 或业务 Handler。
- 每会话发送泵在线程池串行等待异步写入；它不承担游戏逻辑，也不和收包/KCP 更新共用已有网络专用执行器。
- 小型常规消息不为“看起来更异步”额外切序列化线程。只有测量证实序列化/压缩占用主线程预算时，才进入后台编码评估。

### 专用服务器演进目标

连接规模、每秒包量或 CPU 指标证明共享线程池出现饥饿/抖动后，改为有限数量的 I/O 分片，而不是一条全局发送线程：按 `SessionId` 将连接稳定分配到分片；分片负责该连接的收包、协议层和有序写出，不同分片可并行。业务计算、大型快照、压缩或数据库工作仍进入业务工作池，完成后再回到所属 I/O 分片入队发送。

### 序列化进入后台的前置条件

1. 压测已显示序列化、压缩或大包构建是主线程可观测瓶颈，而不是 Socket、队列或业务 Handler。
2. 主线程先生成不可变数据快照；后台线程绝不直接读取 Unity 对象或同时被业务修改的容器。
3. 多个后台编码任务允许乱序完成时，使用会话内序号或有序提交器保证入队顺序。
4. 比较主线程耗时、P99、GC、复制成本和端到端延迟；没有净收益即撤回。

### 验收标准

- 客户端在帧率压力下，网络发送不因 Unity 主线程延迟形成持续出站积压。
- 高并发服务器场景中，慢会话不会使无关会话的 P99 一起恶化。
- 收包、业务处理、编码和发送的跨线程边界均有明确缓冲所有权、容量上限、取消和异常归还语义。

## 阶段 6：生成式 Handler 绑定（已完成，持续监控）

当前实现已在 Editor 编译后扫描 `MiniCore.HotUpdate` 的 `AMHandler<T>` 与 `ARpcHandler<TRequest,TResponse>`，生成 `HotUpdateHandlerRegistry.Generated.cs` 与 `OpcodeRegistry.Generated.cs`。运行时直接构造并注册 Handler，不扫描 AppDomain，不以字符串或 `Activator` 创建 Handler。

### 持续项

- 保持删除/改名 Handler 的首轮安全空表与二次自动生成流程可用。
- 保持 Manifest、Opcode Registry、Handler Registry 的构建前一致性校验。
- 若压测表明字典查询、运行时 Type 反序列化或类型转换成为瓶颈，再评估更细粒度的生成式 Parser/派发绑定。
- 每次调整生成器时验证 IL2CPP/AOT、HotUpdate DLL 与 YooAsset 资源同步。

### 验收标准

- 每包处理路径不出现 `MethodInfo.Invoke`、`DynamicInvoke`、运行时 Handler 扫描或运行时表达式编译。
- 生成内容可重复、可审查，删除 Handler 后不会阻断脚本编译。
- 构建前校验对 Proto、Manifest 和生成注册表给出明确失败信息。

## 阶段 7：Global 生命周期诊断

### 实施项

- 在 Development/Editor 模式记录 owner 获取次数、首次获取栈和持有时长。
- 场景切换或退出时输出未释放 owner 与仍 Pin 的组件清单。
- 为 `Pin` 和 `ForceRemove` 记录调用原因，禁止业务层用 `ForceRemove` 替代常规 `Remove`。
- 定期审查常驻组件：只有确实跨调用者、跨场景持续运行的服务才应 Pin。

### 验收标准

- 能定位“谁持有组件、持有几次、为何未释放”。
- 不在 Release 构建的热路径保留栈追踪或额外 GC。

## 阶段 8：UI、资源与对象池异步治理

### 实施项

- 窗口打开、预加载、资源加载和对象池创建统一使用 MTask Owner 任务域；不在业务方法签名中增加或转发取消令牌。窗口/场景关闭时通过 Owner 释放自动取消，阻塞 I/O 在 `OnDisposing()` 同步解除。
- 同资源路径的并发请求做 in-flight 去重，并以 `.Share()` 明确表达多方等待同一底层加载。
- 给 UI 缓存和对象池设置容量、淘汰和场景卸载策略。
- 异步完成时仍检查 owner、窗口或场景是否有效，避免外部回调写入已销毁对象；不以应用快速退出作为完成回调的可靠时机。

### 验收标准

- 窗口关闭或场景切换后不会出现延迟回调创建孤儿对象。
- 高速打开/关闭窗口时资源加载次数和对象池数量受控。

## 推荐执行顺序

| 顺序 | 阶段 | 原因 |
| --- | --- | --- |
| 1 | 阶段 0 | 没有数据就无法判断优化是否有效。 |
| 2 | 阶段 1 | 控制积压是网络稳定性的基础。 |
| 3 | 阶段 2、3 | 处理高并发时的生命周期、内存和超时压力。 |
| 4 | 阶段 4、5 | 根据实际业务决定协议升级和公网韧性投入。 |
| 5 | 阶段 6 | 持续验证已完成的生成式绑定，不在无数据时继续过度优化。 |
| 6 | 阶段 7、8 | 持续提升可维护性和异步资源正确性。 |

## 每次优化的提交检查单

- [ ] 明确本次问题、负载模型和目标指标。
- [ ] 增加或更新自动化测试，覆盖成功、超限、异常和释放路径。
- [ ] 对比改动前后的吞吐、P95/P99、主线程耗时和 GC。
- [ ] 检查 TCP、UDP、KCP 至少不会发生行为回归。
- [ ] 检查 `ByteBufferPool` 租还对称和队列拒绝路径。
- [ ] 检查 IL2CPP/AOT 与 HotUpdate 兼容性。
- [ ] 更新本文档中的状态、结论和遗留风险。
