# MiniCore 阶段性优化总结

## 2026-08-03 第一阶段（总结时间：2026-08-03 00:33:53，北京时间）

- 正式网络序列化改用 Protobuf：序列化耗时降低 `64.72%`，序列化性能提升 `183.45%`；反序列化耗时降低 `77.72%`，反序列化性能提升 `348.87%`；正文大小降低 `10.10%`。
- 收包缓冲池改为有界数组槽位栈：缓冲池租还耗时降低 `7.17%`，稳定 GC 采样计数降低 `99.95%`，完整收包交接耗时降低 `6.06%`。
- 日志关闭时先判断开关再构造时间与字符串：收包日志热路径耗时降低 `99.84%`，GC 采样计数降低 `100%`。
- RPC 控制入站队列扩容并保持有界：TCP 与 KCP 的 RPC 失败率均降低 `100%`。
- 客户端出站发送泵切换到共享线程池：TCP RPC 吞吐提升 `1330.95%`，P99 降低 `92.91%`，出站排队耗时降低 `97.73%`，传输等待耗时降低 `94.72%`；UDP RPC 吞吐提升 `819.56%`，P99 降低 `85.60%`，出站排队耗时降低 `96.45%`，传输等待耗时降低 `93.10%`。
- TCP 小包将长度头与正文合并为单次写入：`5000/s` 场景接受数提升 `55.01%`，吞吐提升 `41.13%`。
- TCP 普通数据队列使用有界多帧批量写入：`5000/s` 场景接受数提升 `117.40%`，吞吐提升 `120.01%`，P99 降低 `16.63%`。
- TCP 改用连续接收缓冲并循环拆包：`5000/s` 场景接受数提升 `110.10%`，吞吐提升 `133.44%`，P99 降低 `99.44%`，Socket 接收操作数降低 `90.71%`。
- UDP `TrySend` 使用有界数据报合包：`5000/s` 场景接受数提升 `294.72%`，P99 降低 `94.62%`，每逻辑包对应的实际 UDP 写入数降低 `83.10%`。
