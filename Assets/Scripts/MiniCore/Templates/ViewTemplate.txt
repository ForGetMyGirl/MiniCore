using MiniCore.UI;

/// <summary>
/// {VIEW_CLASS} 的被动 Unity View。
/// </summary>
public sealed class {VIEW_CLASS} : AUIWindowView
{
    #region UnityProperty Unity 引用属性

    // 在此声明并由 Prefab 绑定 UGUI 控件。

    #endregion
}
